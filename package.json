{
  "name": "shiryo-kanri",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute shiryo-kanri --remote --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
